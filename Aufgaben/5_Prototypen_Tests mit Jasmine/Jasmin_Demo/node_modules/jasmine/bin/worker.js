const cluster = require('node:cluster');
const ParallelWorker = require('jasmine/lib/parallel_worker');
const Loader = require('jasmine/lib/loader');

new ParallelWorker({
  loader: new Loader(),
  process,
  clusterWorker: cluster.worker
});
